import { Stack } from "expo-router";

export default function Layout() {
  return <Stack />;
}
// This layout component uses the Stack component from expo-router to create a stack navigator for the app.